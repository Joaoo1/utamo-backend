export interface ICreateGutterDTO {
  name: string;
  base: number;
  slope: number;
  maxHeight: number;
  roughness: number;
  maxSpeed: number;
  drainageProjectId: string;
  userCompanyId: string;
}
